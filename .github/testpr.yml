name: Test AKS build for a pr

permissions:
  contents: read
  id-token: write

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test env
        run: |
          echo main


